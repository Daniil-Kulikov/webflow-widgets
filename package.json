{
  "name": "flowfx",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:esm && npm run build:cdn",
    "build:esm": "vite build --emptyOutDir true --mode esm",
    "build:cdn": "vite build --mode cdn",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gsap": "^3.15.0",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
